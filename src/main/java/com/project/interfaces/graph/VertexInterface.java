package com.project.interfaces.graph;

/**
 * Vertex interface: a vertex in the graph
 */
public interface VertexInterface
{
    /**
     * @param element the element to test
     * @return if this == element
     */
    boolean equals(Object element);
}
